name: workflow_env
on:
    workflow_dispatch:
jobs:
    
    build-in-environment:
        runs-on: ubuntu-latest
        steps:
          - name: checkout code
            uses: actions/checkout@v3
          - name: check default env
            run: env
        
